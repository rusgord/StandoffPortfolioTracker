# 🚀 Инструкция по внедрению рынка и справки

## Что было создано?

### ✅ Компоненты Blazor

#### 1. **Market.razor** (`Components/Pages/Market.razor`)
Красивый маркетплейс для просмотра предметов с графиками цен.

**Основные компоненты:**
- 🎨 Gradient фон (фиолетово-голубой)
- 📊 Сетка с карточками предметов
- 🔍 Фильтры (поиск, тип, редкость, коллекция)
- 📈 Интерактивный график истории цен (90 дней)
- 📉 Статистика (мин, макс, средняя цена)
- 📱 Responsive design (работает на мобильных)

**Маршрут:** `/market`

#### 2. **Help.razor** (`Components/Pages/Help.razor`)
Подробная справка и FAQ.

**Основные разделы:**
- 🚀 Начало работы (4 шага)
- ❓ FAQ (8 вопросов в accordion)
- 🔗 Полезные ссылки (4 кнопки)
- 💡 Советы и трюки

**Маршрут:** `/help`

### ✅ Файлы поддержки

#### 1. **wwwroot/js/chart-helper.js**
JavaScript функция для отрисовки графиков с Chart.js.

**Функция:**
```javascript
window.drawPriceChart(dates, prices, itemName)
```

#### 2. **Обновление ItemService.cs**
Добавлен метод для получения истории цен:

```csharp
public async Task<List<(DateTime Date, decimal Price)>> GetPriceHistoryAsync(
    int itemBaseId, 
    int days = 90)
```

---

## 🔧 Как это работает?

### Рынок (Market)

1. **Загрузка данных:**
   - При открытии страницы загружаются все предметы
   - Используется `ItemService.GetItemsFilteredAsync()` с пагинацией (50 шт/страница)
   - Загружаются коллекции для фильтра

2. **Фильтрация:**
   - Поиск: по названию и скину (real-time)
   - Тип: ItemType (Guns, Melee, etc.)
   - Редкость: ItemRarity (Common, Uncommon, Rare, Epic, Legendary, Arcane, Nameless)
   - Коллекция: GameCollection

3. **Выбор предмета:**
   - Клик на карточку выделяет предмет
   - Справа появляется панель с деталями
   - Загружается история цен за 90 дней
   - Вычисляется min/max/avg цена
   - Отрисовывается график с Chart.js

4. **График:**
   - X-ось: Даты (последние 90 дней)
   - Y-ось: Цена в G (золото)
   - Линия: интерактивная с hover-эффектами
   - Градиент под линией: фиолетовый

### Справка (Help)

1. **Структурированная информация:**
   - Левая колонка: Начало работы + FAQ
   - Правая колонка: Ссылки + Советы

2. **FAQ Accordion:**
   - 8 вопросов с развернутыми ответами
   - Первый вопрос открыт по умолчанию
   - Smooth expand/collapse анимация

3. **Навигация:**
   - Быстрые ссылки на другие разделы
   - Прямые ссылки в href атрибутах

---

## 📋 Требования к БД

### Таблица ItemBase
```sql
CREATE TABLE ItemBases (
    Id INT PRIMARY KEY IDENTITY,
    Name NVARCHAR(255) NOT NULL,
    SkinName NVARCHAR(255),
    Rarity INT,
    Type INT,
    Kind INT,
    CollectionId INT,
    ImageUrl NVARCHAR(500),
    CurrentMarketPrice DECIMAL(10,2),
    IsStatTrack BIT DEFAULT 0,
    IsPattern BIT DEFAULT 0
);
```

### Таблица MarketHistory
```sql
CREATE TABLE MarketHistory (
    Id INT PRIMARY KEY IDENTITY,
    ItemBaseId INT NOT NULL,
    Price DECIMAL(10,2),
    RecordedAt DATETIME DEFAULT GETUTCDATE(),
    FOREIGN KEY (ItemBaseId) REFERENCES ItemBases(Id)
);
```

### Таблица GameCollections
```sql
CREATE TABLE GameCollections (
    Id INT PRIMARY KEY IDENTITY,
    Name NVARCHAR(255) NOT NULL,
    ImageUrl NVARCHAR(500),
    IsRemoved BIT DEFAULT 0
);
```

---

## 🎨 Цветовая схема

### Редкости

| Редкость | Цвет | Hex |
|----------|------|-----|
| Common | Серо-голубой | #B0C3D9 |
| Uncommon | Голубой | #5E98D9 |
| Rare | Синий | #4B69FF |
| Epic | Фиолетовый | #8847FF |
| Legendary | Розовый | #D32CE6 |
| Arcane | Красный | #EB4B4B |
| Nameless | Золотой | #FFD700 |

### Основная схема
- Главный: Фиолетовый (#667eea)
- Вторичный: Фиолетовый темный (#764ba2)
- Фон: Градиент от #667eea к #764ba2

---

## 🔌 Интеграция

### Что нужно сделать?

1. ✅ Убедиться, что в БД есть записи в MarketHistory
2. ✅ Настроить автоматическое обновление цен (PriceParserService)
3. ✅ Проверить, что коллекции загружаются корректно

### Автоматическое обновление цен

В **Program.cs** уже должен быть настроен worker:

```csharp
builder.Services.AddHostedService<PriceParserWorker>();
```

Это автоматически обновляет цены и создает записи в MarketHistory.

### Проверка работы

1. Откройте `/market` в браузере
2. Убедитесь, что предметы загружаются
3. Кликните на предмет
4. Проверьте, что график отображается (если есть история цен)

---

## 🐛 Возможные проблемы

### График не отображается
**Причина:** Нет данных в MarketHistory для выбранного предмета
**Решение:** Дождитесь, пока PriceParserWorker обновит цены, или добавьте тестовые данные

### Фильтры не работают
**Причина:** Возможно, ItemService не правильно применяет фильтры
**Решение:** Проверьте, что `GetItemsFilteredAsync()` получает правильные параметры

### Стили не применяются
**Причина:** CSS не загружается
**Решение:** Убедитесь, что `app.css` подключен в App.razor

### Chart.js не работает
**Причина:** CDN недоступен или нет JS файла
**Решение:** 
- Проверьте интернет
- Убедитесь, что `chart-helper.js` находится в `wwwroot/js/`
- Проверьте консоль браузера на ошибки

---

## 📱 Адаптивность

### Режимы отображения

**Desktop (1200px+):**
- 2 колонки: сетка слева, детали справа
- Детали липкие (sticky)

**Планшет (768px - 1200px):**
- Сетка уменьшается
- Детали в 4-х колонках справа

**Мобильный (<768px):**
- Сетка в 2 колонки
- Детали под сеткой (full-width)

---

## 🚀 Оптимизация

### Производительность

- ✅ Пагинация (50 предметов на странице)
- ✅ Ленивая загрузка графиков (только при клике)
- ✅ Кэширование коллекций
- ✅ Оптимизированные SQL запросы

### SEO

- ✅ PageTitle установлены
- ✅ Семантический HTML
- ✅ Иконки через Bootstrap Icons

---

## 📚 Дополнительные ресурсы

### Документация
- **MARKET_HELP_GUIDE.md** - полное руководство пользователя
- **IMPLEMENTATION_CHECKLIST.md** - чеклист реализации

### Библиотеки
- **Chart.js 4.4.0** - графики (CDN)
- **Bootstrap 5** - CSS фреймворк
- **Bootstrap Icons** - иконки

---

## ✨ Что нужно помнить

1. **История цен автоматически создается** - когда PriceParserService обновляет цены
2. **Фильтры работают на сервере** - никаких задержек
3. **График интерактивный** - можно наводить на точки для просмотра цен
4. **Все данные из БД** - ничего не захардкодено

---

**Статус:** ✅ Готово к продакшену  
**Версия:** 1.0  
**Последнее обновление:** 2025
