@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Подтверждение";
    Layout = "/Areas/Identity/Pages/_AuthLayout.cshtml";
}

<div class="auth-header">
    <div class="auth-icon-wrapper" style="background: white; border: 2px solid #e2e8f0; color: #4285f4;">
        <i class="bi bi-google fs-2"></i>
    </div>
    <h3 class="auth-title">Почти готово!</h3>
    <p class="auth-subtitle text-success">
        Вход через <strong>@Model.ProviderDisplayName</strong> успешен.
    </p>
</div>

<div class="auth-body">
    <p class="text-center text-muted mb-4" style="font-size: 0.9rem;">
        Придумайте пароль, чтобы иметь доступ к аккаунту даже без Google.
    </p>

    <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger small mb-3 text-center"></div>

        <div class="form-floating mb-3">
            <input asp-for="Input.Email" class="form-control bg-light" autocomplete="email" readonly />
            <label asp-for="Input.Email">Ваш Email (от Google)</label>
            <span asp-validation-for="Input.Email" class="text-danger small"></span>
        </div>

        <div class="form-floating mb-3">
            <input asp-for="Input.Password" class="form-control" autocomplete="new-password" placeholder="Пароль" />
            <label asp-for="Input.Password">Придумайте пароль</label>
            <span asp-validation-for="Input.Password" class="text-danger small"></span>
        </div>

        <div class="form-floating mb-4">
            <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" placeholder="Пароль еще раз" />
            <label asp-for="Input.ConfirmPassword">Повторите пароль</label>
            <span asp-validation-for="Input.ConfirmPassword" class="text-danger small"></span>
        </div>

        <button type="submit" class="btn-auth-primary">Завершить регистрацию</button>
    </form>

    <div class="auth-footer-links">
        <a asp-page="./Login" class="auth-link text-muted">Отмена</a>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}